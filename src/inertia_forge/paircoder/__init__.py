"""Optional bpsai-pair (PairCoder) evidence backend for `task_management` mode.

Shipped always, but the `task_management` evidence mode shells out to
``bpsai-pair`` at runtime — declare the dependency with the [paircoder] extra:

    pip install inertia-forge[paircoder]
"""
